# pyproject.toml (minimal)
[project]
name = "moe-lab"
version = "0.1.0"
description = "MoE expert tuner with safe device handling + router bias"
requires-python = ">=3.10"
dependencies = ["torch", "transformers", "accelerate", "peft"]

[project.scripts]
moe-lab = "moe_lab:main"
